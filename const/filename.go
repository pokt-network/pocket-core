package _const

var (
	REPORTFILENAME = DATADIR + FILESEPARATOR + "reports.json"
	SNWLFILENAME   = DATADIR + FILESEPARATOR + "service_whitelist.json"
	DWLFILENAME    = DATADIR + FILESEPARATOR + "developer_whitelist.json"
	PEERFILENAME   = DATADIR + FILESEPARATOR + "peers.json"
	CHAINSFILENAME = DATADIR + FILESEPARATOR + "chains.json"
)
